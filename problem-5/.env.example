# Copy this file to .env and update values as needed

# Runtime environment
NODE_ENV=development

# HTTP server port
PORT=3000

# Postgres connection string used by the app and TypeORM CLI
# Format: postgres://<user>:<password>@<host>:<port>/<database>
DATABASE_URL=postgres://postgres:postgres@localhost:5432/postgres

# Optional: enable if your Postgres requires SSL
# PGSSLMODE=require

# Optional: logging level for application logs
# LOG_LEVEL=info

# Optional: CORS origin (add handling in code if you want to use this)
# CORS_ORIGIN=*